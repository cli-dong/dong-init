'use strict';

/**
 * @property {mixture} ****** CONST
 * @see app/index/mixins/const
 */

/**
 * @property {class} REST 实现了 RESTful 的 Base 子类
 * @see app/index/mixins/rest
 */

/**
 * @property {object} route 路由模块
 * @see app/index/mixins/route
 */

/**
 * @property {object} auth 身份验证模块
 * @see app/index/mixins/auth
 */

/**
 * @property {object} user UserData
 * @see app/index/mixins/user
 */

/**
 * @property {function} use 启动指定 app
 * @see app/index/mixins/use
 */

/**
 * @property {function} $ jQuery
 * @see app/index/mixins/dollar
 * @see package.json/spm/buildArgs
 */

/**
 * @property {function} redirect 路由重定向
 * @see app/index/mixins/route
 */

/**
 * @property {function} ajax 通用的 ajax 封装
 * @see app/index/mixins/ajax
 */

/**
 * @method ready
 * @param {function} callback 确保在 util ready 后执行
 * @example
 * util.ready(function() {
 *   // return function destroy() {
 *   //   // for GC
 *   // }
 * });
 */

module.exports = {};
